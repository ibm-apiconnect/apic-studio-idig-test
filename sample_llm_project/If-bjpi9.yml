kind: If
apiVersion: api.ibm.com/v1
metadata:
  name: If-bjpi9
  namespace: TEST_LLM
  version: '1.0'
  tags: []
  labels:
    gatewayTypes:
      - nano
spec:
  condition: ($pathParameter('a_model_id'))
  execute:
    - $ref: TEST_LLM:Set-bjpi9:1.0
