kind: Plan
apiVersion: api.ibm.com/v1
metadata:
  name: plan-join_chatgpt-bjpi9
  namespace: TEST_LLM
  version: '1.0'
  tags: []
  description: ''
spec:
  qos:
    withQuota:
      openai-token-weighted:
        - $ref: TEST_LLM:openai-token-weighted-bjpi9:1.0
  extensions:
    dp-nano-gateway:
      apiVersion: api.ibm.com/v1
      spec:
        apis:
          - $ref: TEST_LLM:api-join_chatgpt-bjpi9:1.0
            operations:
              - path: /chat/completions
                method: post
              - path: /embeddings
                method: post
              - path: /models
                method: get
              - path: /models/{a_model_id}
                method: get
        telemetry: {}
    dp-gateway:
      apiVersion: api.ibm.com/v1
      spec:
        apis:
          - $ref: TEST_LLM:api-join_chatgpt-bjpi9:1.0
            operations:
              - path: /chat/completions
                method: post
              - path: /embeddings
                method: post
              - path: /models
                method: get
              - path: /models/{a_model_id}
                method: get
